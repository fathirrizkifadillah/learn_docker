# Untuk membuat container dari docker compose (harus masuk ke folder yang ada docker composenya)
docker compose create

# Untuk menjalankan container
docker compose start

# Untuk menghentikan container
docker compose stop

# Untuk menghapus container yang ada di docker compose
docker compose down